services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: rush
      POSTGRES_PASSWORD: rush_dev_2026
      POSTGRES_DB: rush_profiles
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
